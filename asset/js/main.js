// Get access to the nav item container from html
const navItemContainer = document.getElementById("nav-items");

// function that opens and close navbar menu
function toggleNavbar() {
  navItemContainer.classList.toggle("open-nav-item");
}
